import { colors } from './colors';

export const theme = {
  colors,
  font: {
    this: 300,
    regular: 400,
    bold: 800,
  },
  media: {
    phone: '@media(min-width: 1024px)',
    desktop: '@media(min-width: 724px)',
  },
};
